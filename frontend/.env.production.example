# Production environment variables (frontend / Vite)
# This file is safe to commit if it contains no secrets.

VITE_API_URL=/api

# Pusher Configuration (Real-time notifications)
# These are PUBLIC keys, safe to expose in client-side code
VITE_PUSHER_KEY=your_app_key
VITE_PUSHER_CLUSTER=us2